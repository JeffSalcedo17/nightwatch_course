module.exports = {
  // An array of folders (excluding subfolders) where your tests are located;
  // if this is not specified, the test source must be passed as the second argument to the test runner.
  src_folders: ['tests'],

  webdriver: {
    start_process: true,
    server_path: 'C:/Users/Yefferson.Salcedo/Documents/Courses/API-UI-Automation-with-NightWatch-JS/First Test/node_modules/.bin/chromedriver',
	port: 4444
  },

  test_settings: {
    default: {
      desiredCapabilities : {
        browserName : 'chrome'
      }
    }
  }
};
